# Phase 1 Prompt

Task:
Stabilize the zero-dependency starter app.

Run:
```bash
npm run codex:check
```

Fix only concrete validation or build errors.

Acceptance:
- validation passes
- static build passes
- no external npm dependency is added
- no generated files are committed
- visual direction remains aligned with reference/approved_reference.svg

Stop after Phase 1 and report results.
