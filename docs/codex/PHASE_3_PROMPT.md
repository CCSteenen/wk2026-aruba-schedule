# Phase 3 Prompt

Task:
Create export planning around the existing poster route and approved Python renderer.

Goals:
- document how `/poster` should become the canonical export route
- preserve reference/render_wk2026_aruba.py as the approved export reference
- do not add Playwright or any external dependency unless explicitly approved

Run:
```bash
npm run codex:check
```

Stop after Phase 3.
