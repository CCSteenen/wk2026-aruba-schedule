export function ExportCenter() {
  return (
    <section className="pagePanel">
      <h2>Export Center</h2>
      <p>
        This starter app keeps export planning inside the UI. The approved local renderer remains in
        <code> reference/render_wk2026_aruba.py</code>. The next Codex phase can connect the
        browser <code>/poster</code> route to SVG, PNG and PDF exports.
      </p>
      <ul>
        <li>Master poster PNG: 3840 × 2160</li>
        <li>Editable SVG poster</li>
        <li>Print PDF</li>
        <li>QA report</li>
      </ul>
    </section>
  );
}
